import { FaktumForm } from "@/components/case/FaktumForm";

export const metadata = {
  title: "Ny sak | Lovsentralen",
  description: "Beskriv din juridiske situasjon for å få veiledning",
};

export default function FaktumPage() {
  return <FaktumForm />;
}
